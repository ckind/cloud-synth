import { IPreset } from "../presets/IPreset";
import { IPresetBank } from "../presets/IPresetBank";
import { IPresetCategory } from "../presets/IPresetCategory";

export interface IDeviceContainer {
  device: any;
  // currentPreset: IPreset;
  // currentBank: IPresetBank;
  // loadPreset(id: string): void;
  // loadBank(id: string): void;
  // saveCurrentPreset(): void;
  // saveCurrentPresetAs(): void;
}